# Running the System

## View Locally
To view the application locally, you can use a simple HTTP server:

```bash
python3 -m http.server 8000 --directory dist
```

Alternatively, for development:

```bash
npm install
npm run dev
```
